# Get Cloud Performance Ratio

Returns estimated per-core performance of cloud vs local hardware. Based
on empirical benchmarking data.

## Usage

``` r
get_cloud_performance_ratio(local_specs, platform)
```
