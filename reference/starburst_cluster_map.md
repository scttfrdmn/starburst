# Execute Map on Starburst Cluster

Internal function to execute parallel map by creating StarburstFuture
objects directly

## Usage

``` r
starburst_cluster_map(cluster, .x, .f, .progress = TRUE)
```
