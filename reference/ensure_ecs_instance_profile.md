# Ensure ECS instance IAM profile exists

Ensure ECS instance IAM profile exists

## Usage

``` r
ensure_ecs_instance_profile(region)
```

## Arguments

- region:

  AWS region

## Value

Instance profile ARN
