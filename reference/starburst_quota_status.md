# Show quota status

Show quota status

## Usage

``` r
starburst_quota_status(region = NULL)
```

## Arguments

- region:

  AWS region (default: from config)

## Examples

``` r
if (FALSE) { # \dontrun{
starburst_quota_status()
} # }
```
