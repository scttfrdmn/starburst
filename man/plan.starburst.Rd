% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-starburst.R
\name{plan.starburst}
\alias{plan.starburst}
\title{staRburst Future Backend}
\usage{
\method{plan}{starburst}(
  strategy,
  workers = 10,
  cpu = 4,
  memory = "8GB",
  region = NULL,
  timeout = 3600,
  auto_quota_request = interactive(),
  launch_type = "FARGATE",
  instance_type = "c7g.xlarge",
  use_spot = FALSE,
  warm_pool_timeout = 3600,
  detached = FALSE,
  ...
)
}
\arguments{
\item{strategy}{The starburst strategy marker (ignored, for S3 dispatch)}

\item{workers}{Number of parallel workers}

\item{cpu}{vCPUs per worker (1, 2, 4, 8, or 16)}

\item{memory}{Memory per worker (supports GB notation, e.g., "8GB")}

\item{region}{AWS region (default: from config or "us-east-1")}

\item{timeout}{Maximum runtime in seconds (default: 3600)}

\item{auto_quota_request}{Automatically request quota increases (default: interactive())}

\item{launch_type}{Launch type: FARGATE or EC2 (default: FARGATE)}

\item{instance_type}{EC2 instance type when using EC2 launch type (default: c7g.xlarge)}

\item{use_spot}{Use EC2 Spot instances for cost savings (default: FALSE)}

\item{warm_pool_timeout}{Timeout for warm pool in seconds (default: 3600)}

\item{detached}{Use detached session mode (deprecated, use starburst_session instead)}

\item{...}{Additional arguments passed to future backend}
}
\value{
A future plan object
}
\description{
A future backend for running parallel R workloads on AWS Fargate
}
\examples{
\dontrun{
library(furrr)
plan(starburst, workers = 50)
results <- future_map(1:1000, expensive_function)
}
}
